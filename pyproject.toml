[project]
name = "editorial-agent-graph"
version = "0.1.0"
description = "Multi-agent editorial pipeline built with LangGraph: topic discovery, parallel news research with grounded search, human-in-the-loop approval, and draft generation."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "langchain>=1.2.15",
    "langchain-google-genai>=4.2.1",
]
